# NetSpeed-Lite Configuration
# Copy this file to .env and customize

# ============================================================================
# Server Configuration (optional - default is fine for most users)
# ============================================================================

# HTTP server bind address (default: 0.0.0.0:9109)
# Change to 127.0.0.1:9109 for localhost-only access
# NETSPEED_BIND=0.0.0.0:9109

# ============================================================================
# Scheduling Configuration
# ============================================================================

# Scheduling Mode: hourly_aligned (default), interval, or cron
NETSPEED_SCHEDULE_MODE=hourly_aligned

# Option 1: Hourly Aligned (default if no mode specified)
# Runs exactly at the top of the hour (e.g. 10:00, 11:00)
# No extra config needed

# Option 2: Interval
# Runs every X seconds regardless of clock time
# NETSPEED_SCHEDULE_MODE=interval
# NETSPEED_INTERVAL_SECONDS=3600

# Option 3: Cron (Advanced)
# Uses standard cron expression (second minute hour day month weekday)
# NETSPEED_SCHEDULE_MODE=cron
# NETSPEED_SCHEDULE=0 0 * * * *

# Timezone for scheduling (default: Europe/London)
# Use IANA timezone names: America/New_York, Asia/Tokyo, Europe/Paris, etc.
NETSPEED_TIMEZONE=Europe/London

# Allow overlapping runs if previous test hasn't finished (default: false)
# NETSPEED_ALLOW_OVERLAP=false

# ============================================================================
# Notification Configuration
# ============================================================================

# When to send notifications: success, failure, or both (default: success,failure)
# Options: success, failure, success,failure
NETSPEED_NOTIFY_ON=success,failure

# ntfy topic URL (REQUIRED for notifications)
# Example: https://ntfy.sh/my-speedtest-topic
# For self-hosted: https://ntfy.example.com/my-topic
NETSPEED_NTFY_URL=https://ntfy.sh/my-speedtest-alerts

# ntfy authentication token (optional)
# Use for private topics or authentication
# NETSPEED_NTFY_TOKEN=your_token_here

# Notification title (default: netspeed-lite)
NETSPEED_NTFY_TITLE=netspeed-lite

# Notification tags (default: speedtest,isp)
# Comma-separated list of tags
NETSPEED_NTFY_TAGS=speedtest,isp

# Notification priority (default: 3)
# Range: 1 (min) to 5 (max)
NETSPEED_NTFY_PRIORITY=3

# URL to open when notification is clicked (optional)
# Example: https://grafana.example.com/d/netspeed
# NETSPEED_NTFY_CLICK=

# ============================================================================
# Logging Configuration (optional)
# ============================================================================

# Rust log level (default: info)
# Options: trace, debug, info, warn, error
RUST_LOG=info
